Algorithms which aim to keep the 'current' state and try to improve it (get closer to the goal state)

Often, these algorithms only require constant space since we only keep track of the current state