This is what happens when the CPU switches to another [[process]]
### Process
- The system saves the state of the current process in the [[PCB]] to main memory
- The system loads the saved state of the new process